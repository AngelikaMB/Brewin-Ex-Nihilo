import React from 'react'


export default function CityGen(props) {


function generate(array) {
  return  array[Math.floor(Math.random() * array.length)];
}

    return (
        <div>
        
        </div>
    )
}